import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    padding: 16,
    flexDirection: 'row',
  },
  button: {
    marginVertical: 16,
    marginHorizontal: 24,
    alignItems: 'center',
  },
});

export default styles;

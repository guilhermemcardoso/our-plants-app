import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  xpContainer: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
  },
  xp: {
    textAlign: 'center',
    fontWeight: 'bold',
  },
  levelContainer: {},
});

export default styles;

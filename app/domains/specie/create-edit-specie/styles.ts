import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  infoContainer: {
    padding: 16,
    borderRadius: 8,
    marginTop: 16,
  },
  sectionLabel: {
    marginBottom: 10,
    textAlign: 'center',
    fontWeight: 'bold',
  },
  createEditButton: {
    marginTop: 16,
    marginBottom: 30,
  },
  input: {
    marginTop: 16,
  },
});

export default styles;

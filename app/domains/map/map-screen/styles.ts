import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mapContainer: {
    flex: 1,
    marginBottom: 68,
  },
  map: {
    borderRadius: 8,
  },
});

export default styles;

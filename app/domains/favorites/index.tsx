import React from 'react';
import { Button, SafeAreaView } from 'react-native';

const Favorites = () => {
  return (
    <SafeAreaView>
      <Button onPress={() => {}} title="Favorites" />
    </SafeAreaView>
  );
};

export default Favorites;
